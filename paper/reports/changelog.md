# Honesty-First Revision Changelog

| Metric/Claim | Old Text / Issue | New Text / Framing | Source File / Verification |
| :--- | :--- | :--- | :--- |
| **Crop Recommendation Accuracy** | Manuscript inconsistently reported 97.86%, 98.05%, 99.54% across sections. | Reconciled to exactly **98.05%** across the abstract, results, and discussion. Explicitly labeled as the result *under simulated Gaussian noise*. | `run_all_experiments.py` line 107 -> `final_results.json` (`RF_Accuracy_Mean`) |
| **Dynamic Pricing R²** | Framed positively as an R² of 0.984 (an artifact of data leakage prior to the audit), hiding the negative performance. | Explicitly states: "achieving an absolute test R² of **-0.25**". Reframes the ablation study as reducing variance error (RMSE 190.21 -> 189.17) rather than absolute success, citing missing macro features. | `run_all_experiments.py` line 345 -> `final_results.json` (`Test_R2_Mean`) |
| **Simulated Economic Impact** | "82.02% increase in farmer profit margins" presented without immediate qualification as a Monte Carlo projection. | Appended "projected" or "simulated" to every mention of the 82.02% figure. Added explicit disclaimer that this is unverified by physical pilot deployment. | `simulate_economic_impact.py` terminal output. |
| **SUS/TAM Generalizability** | Described in language that implied real-world validation of the platform. | Clarified as a "single-site task-based lab evaluation". Added note in Limitations that true usability requires a field pilot. | `generate_sus_tam_data.py` (Script only generated lab-task surveys) |
| **IoT Sensor Noise** | Implied physical IoT hardware testing ("evaluate real-world robustness against cheap IoT sensors"). | Clarified that noise was "simulated via injected Gaussian noise on existing dataset features, not tested on physical IoT hardware". | `run_all_experiments.py` line 42 (Gaussian noise injection) |
| **Demand Forecasting Metrics** | Relied on generic $p < 0.05$ claims. | Updated with exact values: MAE = **34.92**, $R^2$ = **0.088**, $p$-value = $1.74 \times 10^{-5}$, Cohen's $d = 10.80$. | `final_results.json`, `final_statistical_tests.json` |
| **Broken Figures** | Referenced `figures/architecture.png` and `figures/pipeline.png` which did not exist. | Wrote a Python script to programmatically generate accurate, non-fabricated `matplotlib` diagrams of the actual architecture and pipeline. | `experiments/generate_figures.py` |
| **Reproducibility Manifest** | No `requirements.txt` existed; instructions were unverified. | `requirements.txt` was generated via `pip freeze`. `REPRODUCE.md` was rewritten to list the exact command execution order required to replicate every number in the manuscript. | `requirements.txt`, `REPRODUCE.md` |
